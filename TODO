* TODO: set up a separate double fault handler (see OSDev / Triple_Fault)
* TODO: call invalidate_tlb() whenever appropriate!
* TODO: optimize memset() and memcpy() by setting/copying more than 1 byte at a time! (rep movsd?)
